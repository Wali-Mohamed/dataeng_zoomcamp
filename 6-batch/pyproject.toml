[project]
name = "6-batch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.10.*"
dependencies = [
    "pandas>=2.3.3",
    "pyspark==3.5.0",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
